# Falta aqui `observabilidad.mock.json`

No esta en el repositorio a proposito: trae nombres y apellidos de
directores, product owners y service owners, y este repositorio es publico.

## Hace falta?

No para que el sitio funcione. Es el respaldo que usa `observabilidad.js` cuando los
handlers no responden -desarrollo sin base de datos-. Con IIS y la base
arriba, el sitio lee de `Tickets_Proactivanet` y este archivo nunca se pide.

## Como conseguirlo

Pidelo al equipo; no se manda por el repositorio. Va en esta misma carpeta
con el nombre `observabilidad.mock.json`.
